<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mapper.SourceFileMapper">
    <insert id="insertSourceFile" parameterType="POJO.SourceFile">

        insert into file(text_name,userid,time,money,token1,token2)VALUES (
        #{text_name},#{userid},#{time}

        )



    </insert>
</mapper>